# 🚀 Быстрый деплой на Railway

## 📝 Пошаговая инструкция

### 1. Подготовка (5 минут)

```bash
# Убедитесь, что проект собирается
npm run build

# Если все ок - продолжаем
```

### 2. Загрузка в GitHub (если еще не загружено)

```bash
git init
git add .
git commit -m "Ready for Railway deploy"
git branch -M main
git remote add origin https://github.com/ваш-username/Panda-vpn.git
git push -u origin main
```

### 3. Деплой на Railway (10 минут)

1. **Зайдите на [railway.app](https://railway.app)**
2. **Войдите через GitHub**
3. **Нажмите "New Project"**
4. **Выберите "Deploy from GitHub repo"**
5. **Выберите репозиторий `Panda-vpn`**
6. **Railway автоматически начнет деплой**

### 4. Настройка переменных окружения

В Railway Dashboard:

1. Settings → Variables
2. Add Variable:
   - **Name:** `NEXT_PUBLIC_API_URL`
   - **Value:** `http://103.74.92.81:8000` (или ваш бэкенд URL)
3. Нажмите Add

### 5. Получение домена

1. Settings → Networking
2. Нажмите **"Generate Domain"**
3. Получите домен вида: `panda-vpn-production.up.railway.app`

### 6. Готово! 🎉

Ваш сайт доступен по домену Railway!

---

## 🔧 Настройка кастомного домена (опционально)

1. Settings → Networking → Custom Domain
2. Введите ваш домен (например: `pandavpn.com`)
3. Настройте DNS записи (Railway покажет инструкции)
4. Дождитесь активации (обычно 5-15 минут)

---

## ⚠️ Важно

### Обновите CORS на бэкенде:

Добавьте домен Railway в `CORS_ORIGINS`:

```env
CORS_ORIGINS=https://ваш-проект.up.railway.app,https://pandavpn.com
```

---

## 🐛 Проблемы?

### Сайт не открывается:
- Проверьте логи в Railway Dashboard
- Убедитесь, что деплой завершился успешно

### API не работает:
- Проверьте переменную `NEXT_PUBLIC_API_URL`
- Убедитесь, что бэкенд доступен
- Проверьте CORS настройки

---

**Время деплоя:** ~10-15 минут  
**Стоимость:** Бесплатно (до $5 кредитов/месяц)

