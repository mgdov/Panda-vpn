# 🔧 Настройка переменных окружения для фронтенда

## Проблема

Фронтенд обращается к `http://103.74.92.81:8000` вместо `https://vpn-p.ru/api`, что вызывает ошибку Mixed Content.

## Решение

### 1. Создать файл `.env.production` на сервере

```bash
cd ~/Panda-vpn
echo "NEXT_PUBLIC_API_URL=https://vpn-p.ru" > .env.production
```

### 2. Пересобрать фронтенд

```bash
npm run build
pm2 restart panda-vpn-frontend --update-env
```

### 3. Проверить, что переменная установлена

```bash
# Проверить файл
cat .env.production

# Проверить в PM2
pm2 env panda-vpn-frontend | grep NEXT_PUBLIC_API_URL
```

## Что исправлено в коде

1. ✅ `app/api/[...path]/route.ts` - убран хардкод `http://103.74.92.81:8000`
2. ✅ `next.config.ts` - убран хардкод, добавлен `/api` в destination
3. ✅ `lib/api/config.ts` - уже был правильный fallback `https://vpn-p.ru`

## Проверка

После пересборки откройте в браузере:
- `https://vpn-p.ru/auth/signup`
- Откройте DevTools → Network
- Попробуйте зарегистрироваться
- Запрос должен идти на `https://vpn-p.ru/api/auth/register`, а не на `http://103.74.92.81:8000`

