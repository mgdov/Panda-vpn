# Panda VPN - Frontend

Frontend приложение для Panda VPN на Next.js 16.

## 🚀 Быстрый старт

### Требования

- Node.js 18+ (рекомендуется 20+)
- npm, yarn или pnpm

### Установка и запуск

#### Вариант 1: Автоматическая настройка (рекомендуется)

1. **Клонируйте репозиторий** (если еще не сделали):
   ```bash
   git clone <repository-url>
   cd Panda-vpn
   ```

2. **Запустите скрипт настройки**:
   ```bash
   ./setup.sh
   ```
   
   Скрипт автоматически:
   - Проверит версию Node.js
   - Установит зависимости
   - Создаст `.env.local` из `.env.example`

3. **Настройте переменные окружения** (если нужно):
   
   Откройте `.env.local` и при необходимости измените URL бэкенда:
   ```env
   NEXT_PUBLIC_API_URL=http://localhost:8000
   ```

4. **Запустите dev сервер**:
   ```bash
   npm run dev
   # или
   yarn dev
   # или
   pnpm dev
   ```

5. **Откройте браузер**:
   
   Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

#### Вариант 2: Ручная настройка

1. **Клонируйте репозиторий**:
   ```bash
   git clone <repository-url>
   cd Panda-vpn
   ```

2. **Установите зависимости**:
   ```bash
   npm install
   # или
   yarn install
   # или
   pnpm install
   ```

3. **Создайте файл с переменными окружения**:
   ```bash
   cp .env.example .env.local
   ```

4. **Настройте переменные окружения**:
   
   Откройте `.env.local` и укажите URL вашего бэкенда:
   ```env
   NEXT_PUBLIC_API_URL=http://localhost:8000
   ```
   
   Для production используйте:
   ```env
   NEXT_PUBLIC_API_URL=https://vpn-p.ru
   ```

5. **Запустите dev сервер**:
   ```bash
   npm run dev
   # или
   yarn dev
   # или
   pnpm dev
   ```

6. **Откройте браузер**:
   
   Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

## 📝 Доступные команды

- `npm run dev` - запуск dev сервера с hot reload
- `npm run build` - сборка production версии
- `npm run start` - запуск production сервера
- `npm run lint` - проверка кода линтером

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|-----------|----------|----------------------|
| `NEXT_PUBLIC_API_URL` | URL бэкенда API | `https://vpn-p.ru` |

### Локальная разработка

Для локальной разработки создайте файл `.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:8000
```

**Важно**: 
- Файл `.env.local` не попадает в git (уже в .gitignore)
- Используйте `.env.example` как шаблон
- Не коммитьте реальные значения переменных окружения

## 🏗️ Структура проекта

```
Panda-vpn/
├── app/                    # Next.js App Router страницы
│   ├── auth/              # Страницы аутентификации
│   ├── dashboard/         # Личный кабинет
│   └── api/               # API routes (прокси к бэкенду)
├── components/            # React компоненты
│   ├── shared/           # Общие компоненты
│   └── ui/               # UI компоненты (shadcn/ui)
├── lib/                   # Утилиты и конфигурация
│   ├── api/              # API клиент и типы
│   └── utils/            # Вспомогательные функции
├── hooks/                 # React хуки
└── public/                # Статические файлы
```

## 🔌 API Integration

Фронтенд использует прокси через Next.js API routes (`/app/api/[...path]/route.ts`), который перенаправляет запросы на бэкенд.

Все API запросы идут через `lib/api/client.ts`, который автоматически:
- Добавляет JWT токены в заголовки
- Обновляет токены при истечении
- Обрабатывает ошибки

## 🛠️ Технологии

- **Next.js 16** - React фреймворк
- **TypeScript** - типизация
- **Tailwind CSS 4** - стилизация
- **shadcn/ui** - UI компоненты
- **React 19** - UI библиотека

## 📦 Установка зависимостей

Проект использует npm, но также поддерживает yarn и pnpm:

```bash
# npm
npm install

# yarn
yarn install

# pnpm
pnpm install
```

## 🐛 Troubleshooting

### Проблемы с подключением к API

1. Проверьте, что бэкенд запущен и доступен
2. Убедитесь, что `NEXT_PUBLIC_API_URL` указан правильно в `.env.local`
3. Проверьте CORS настройки на бэкенде

### Ошибки при сборке

1. Удалите `.next` и `node_modules`:
   ```bash
   rm -rf .next node_modules
   npm install
   ```

2. Проверьте версию Node.js (должна быть 18+)

### Проблемы с типами TypeScript

Проект настроен на игнорирование ошибок типов при сборке (`ignoreBuildErrors: true`), но для разработки рекомендуется исправлять ошибки.

## 📚 Дополнительная информация

- [Next.js Documentation](https://nextjs.org/docs)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [shadcn/ui Components](https://ui.shadcn.com/)

## 🤝 Разработка

1. Создайте ветку для новой фичи
2. Внесите изменения
3. Проверьте, что все работает локально
4. Создайте Pull Request

## 📄 Лицензия

Private project

