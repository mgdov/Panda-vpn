// PM2 Configuration Example
// Скопируйте этот файл в ecosystem.config.js и настройте под ваш сервер

module.exports = {
  apps: [{
    name: 'panda-vpn',
    script: 'node_modules/next/dist/bin/next',
    args: 'start',
    cwd: '/var/www/panda-vpn', // Измените на путь к вашему проекту
    instances: 1,
    exec_mode: 'fork',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    },
    error_file: '/var/log/panda-vpn/error.log',
    out_file: '/var/log/panda-vpn/out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true,
    autorestart: true,
    max_memory_restart: '1G'
  }]
}

