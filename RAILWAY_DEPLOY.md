# 🚂 Деплой на Railway

## 📋 Подготовка проекта

### 1. Убедитесь, что проект готов к деплою

- ✅ Все зависимости установлены
- ✅ Проект собирается без ошибок: `npm run build`
- ✅ Есть файл `package.json` с правильными скриптами

---

## 🚀 Деплой на Railway

### Способ 1: Через GitHub (Рекомендуется)

#### Шаг 1: Загрузите код в GitHub

```bash
# Если еще не сделали
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/Panda-vpn.git
git push -u origin main
```

#### Шаг 2: Подключите Railway к GitHub

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите репозиторий `Panda-vpn`
6. Railway автоматически определит Next.js и начнет деплой

#### Шаг 3: Настройте переменные окружения

В настройках проекта Railway добавьте переменные:

```env
NEXT_PUBLIC_API_URL=https://ваш-бэкенд-домен.com
# или
NEXT_PUBLIC_API_URL=http://103.74.92.81:8000
```

**Где найти:**
- Settings → Variables → Add Variable

---

### Способ 2: Через Railway CLI

#### Шаг 1: Установите Railway CLI

```bash
npm i -g @railway/cli
```

#### Шаг 2: Войдите в Railway

```bash
railway login
```

#### Шаг 3: Инициализируйте проект

```bash
railway init
```

#### Шаг 4: Задеплойте

```bash
railway up
```

---

## ⚙️ Настройка переменных окружения

### Обязательные переменные:

```env
NEXT_PUBLIC_API_URL=http://103.74.92.81:8000
```

### Опциональные переменные:

```env
# Для production бэкенда
NEXT_PUBLIC_API_URL=https://api.pandavpn.com

# Node окружение (устанавливается автоматически)
NODE_ENV=production
```

**Как добавить:**
1. В Railway Dashboard → Ваш проект
2. Settings → Variables
3. Add Variable
4. Введите `NEXT_PUBLIC_API_URL` и значение
5. Нажмите Add

---

## 🌐 Настройка домена

### Шаг 1: Получите домен Railway

1. В Railway Dashboard → Ваш проект
2. Settings → Networking
3. Нажмите **"Generate Domain"**
4. Railway создаст домен вида: `ваш-проект.up.railway.app`

### Шаг 2: Настройте кастомный домен (опционально)

1. В Settings → Networking
2. Нажмите **"Custom Domain"**
3. Введите ваш домен (например: `pandavpn.com`)
4. Railway покажет DNS записи для настройки

**DNS записи:**
```
Type: CNAME
Name: @
Value: ваш-проект.up.railway.app
```

Или:

```
Type: A
Name: @
Value: IP адрес Railway (Railway покажет)
```

### Шаг 3: Обновите переменные окружения

Если используете кастомный домен, обновите:

```env
NEXT_PUBLIC_API_URL=https://api.pandavpn.com
```

---

## 🔧 Настройка Next.js для production

### Проверьте `next.config.mjs`:

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true, // Для production лучше false
  },
  images: {
    unoptimized: true, // Для Railway можно оставить true
  },
  // Для Railway
  output: 'standalone', // Опционально, для оптимизации
}

export default nextConfig
```

### Обновите `package.json`:

Убедитесь, что есть скрипты:

```json
{
  "scripts": {
    "build": "next build",
    "start": "next start",
    "dev": "next dev"
  }
}
```

---

## 📝 Проверка деплоя

### После деплоя проверьте:

1. ✅ **Сайт открывается** по домену Railway
2. ✅ **API запросы работают** (проверьте в DevTools → Network)
3. ✅ **Авторизация работает** (попробуйте войти)
4. ✅ **Все страницы загружаются**

### Логи Railway:

1. В Railway Dashboard → Ваш проект
2. Deployments → Выберите последний деплой
3. View Logs

**Проверьте:**
- Нет ошибок при сборке
- Сервер запустился успешно
- Порт 3000 слушается

---

## 🐛 Решение проблем

### Проблема: Сборка падает

**Решение:**
```bash
# Проверьте локально
npm run build

# Если есть ошибки - исправьте
# Railway покажет те же ошибки в логах
```

### Проблема: Сайт не открывается

**Решение:**
1. Проверьте логи в Railway
2. Убедитесь, что порт правильный (Railway автоматически определяет)
3. Проверьте переменные окружения

### Проблема: API запросы не работают

**Решение:**
1. Проверьте `NEXT_PUBLIC_API_URL` в переменных окружения
2. Убедитесь, что бэкенд доступен
3. Проверьте CORS настройки на бэкенде

### Проблема: Ошибки CORS

**Решение:**
На бэкенде добавьте домен Railway в CORS:

```env
CORS_ORIGINS=https://ваш-проект.up.railway.app,https://pandavpn.com
```

---

## 🔄 Обновление деплоя

### Автоматический деплой (через GitHub):

1. Сделайте изменения в коде
2. Закоммитьте и запушьте в GitHub:
   ```bash
   git add .
   git commit -m "Update"
   git push
   ```
3. Railway автоматически задеплоит новую версию

### Ручной деплой (через CLI):

```bash
railway up
```

---

## 💰 Стоимость Railway

### Бесплатный план:
- ✅ $5 кредитов в месяц
- ✅ Достаточно для небольшого проекта
- ✅ Автоматический деплой из GitHub

### Платные планы:
- Developer: $5/месяц
- Team: $20/месяц
- Enterprise: по запросу

---

## 📊 Мониторинг

### В Railway Dashboard:

1. **Metrics** - использование ресурсов
2. **Logs** - логи приложения
3. **Deployments** - история деплоев
4. **Settings** - настройки проекта

---

## 🔐 Безопасность

### Рекомендации:

1. ✅ Не коммитьте `.env.local` в Git
2. ✅ Используйте переменные окружения в Railway
3. ✅ Настройте HTTPS (Railway делает автоматически)
4. ✅ Регулярно обновляйте зависимости

---

## 📚 Полезные команды Railway CLI

```bash
# Войти
railway login

# Инициализировать проект
railway init

# Задеплоить
railway up

# Посмотреть логи
railway logs

# Открыть проект в браузере
railway open

# Посмотреть переменные окружения
railway variables

# Добавить переменную
railway variables set NEXT_PUBLIC_API_URL=https://api.example.com
```

---

## ✅ Чеклист перед деплоем

- [ ] Код загружен в GitHub
- [ ] Проект собирается локально (`npm run build`)
- [ ] Все зависимости в `package.json`
- [ ] Переменные окружения подготовлены
- [ ] `railway.json` создан (опционально)
- [ ] `.railwayignore` создан (опционально)
- [ ] Бэкенд доступен и CORS настроен

---

## 🎯 Быстрый старт

1. **Загрузите код в GitHub**
2. **Зайдите на railway.app и войдите через GitHub**
3. **Создайте новый проект из GitHub репозитория**
4. **Добавьте переменную `NEXT_PUBLIC_API_URL`**
5. **Дождитесь деплоя (обычно 2-5 минут)**
6. **Получите домен Railway или настройте кастомный**

---

**Готово!** Ваш фронтенд будет доступен по домену Railway 🚀

**Версия:** 1.0  
**Дата:** 2024-11-18

